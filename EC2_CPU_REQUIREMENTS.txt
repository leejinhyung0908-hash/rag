# FastAPI 및 웹 서버
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# 데이터베이스
psycopg[binary]>=3.2.0
asyncpg>=0.30.0
pgvector>=0.3.6

# LangChain
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-postgres>=0.0.1

# Transformers 및 모델 (CPU 전용)
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.30.0
peft>=0.10.0

# 주의: bitsandbytes는 GPU 전용이므로 제외됨
# CPU 환경에서는 양자화 없이 실행됨 (코드에서 자동 처리)
